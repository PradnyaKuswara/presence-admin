---
import SidebarReact from './Sidebar';
import { QueryProvider } from '../../rests/QueryProvider';

const currentPath = Astro.url.pathname;
---

<QueryProvider client:load>
  <SidebarReact client:load currentPath={currentPath} />
</QueryProvider>
